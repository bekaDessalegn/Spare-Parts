import 'package:flutter/material.dart';

class MultipleSignup extends StatelessWidget {
  const MultipleSignup({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(30),
      child: Column(
        children: [

        ],
      ),
    );
  }
}
