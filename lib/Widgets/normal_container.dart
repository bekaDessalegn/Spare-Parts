import 'package:flutter/material.dart';

class NormalContainer extends StatelessWidget {
  const NormalContainer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Flexy Container"),
      ),
    );
  }
}
